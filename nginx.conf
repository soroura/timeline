# ⚠️ NOT USED in this deployment.
#
# This config was for direct Nginx setup. The actual deployment uses:
#   - Nginx Proxy Manager (UI) to route traffic
#   - A Docker container running nginx:alpine to serve the static files
#
# See DEPLOY.md and docker-compose.yml for the current setup.
